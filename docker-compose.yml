version: '3.8'

services:
  homelab-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: homelab-dashboard
    ports:
      - "4000:80"
    restart: unless-stopped
    networks:
      - homelab

networks:
  homelab:
    driver: bridge
